# SPDX-License-Identifier: MIT
# Proxmox HA Role - handlers/main.yml

- name: reload ha config
  ansible.builtin.command: ha-manager config
  changed_when: true

- name: restart ha-manager
  ansible.builtin.systemd:
    name: ha-manager
    state: restarted
